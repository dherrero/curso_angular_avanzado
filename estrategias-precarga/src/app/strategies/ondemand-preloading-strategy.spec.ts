import { OndemandPreloadingStrategy } from './ondemand-preloading-strategy';

describe('OndemandPreloadingStrategy', () => {
  it('should create an instance', () => {
    expect(new OndemandPreloadingStrategy()).toBeTruthy();
  });
});
