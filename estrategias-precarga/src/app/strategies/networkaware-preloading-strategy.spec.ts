import { NetworkawarePreloadingStrategy } from './networkaware-preloading-strategy';

describe('NetworkawarePreloadingStrategy', () => {
  it('should create an instance', () => {
    expect(new NetworkawarePreloadingStrategy()).toBeTruthy();
  });
});
