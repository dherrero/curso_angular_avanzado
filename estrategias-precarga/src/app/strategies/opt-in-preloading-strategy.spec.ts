import { OptInPreloadingStrategy } from './opt-in-preloading-strategy';

describe('OptInPreloadingStrategy', () => {
  it('should create an instance', () => {
    expect(new OptInPreloadingStrategy()).toBeTruthy();
  });
});
